{% set name = "chemcat" %}
{% set version = "0.3.6" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/chemcat-{{ version }}.tar.gz
  sha256: 042ac50a16332f9803dec34b74b67db3d160d8290aec049775c5b4309fa66790

build:
  script: {{ PYTHON }} -m pip install . -vv
  number: 1
  skip: true  # [win]
requirements:
  build:
    - {{ compiler('c') }}
  host:
    - numpy
    - pip
    - python
  run:
    - matplotlib-base >=3.3.4
    - more-itertools >=8.4.0
    - numpy >=1.19.1
    - python
    - scipy >=1.5.2
    - {{ pin_compatible('numpy') }}

test:
  imports:
    - chemcat
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/atmolib/chemcat
  summary: Chemistry Calculator for Atmospheres
  license: GPL-2.0
  license_file:
    - LICENSE
  dev_url: https://github.com/atmolib/chemcat
  doc_url: https://chemcat.readthedocs.io/en/latest/

extra:
  recipe-maintainers:
    - pcubillos
